Supermarket Food:

foodTable (ean varchar(20), name varchar(80), [fields int, split into c, t and n]);

Food Services:

foodService (foodServiceID varchar(20), name varchar(80), description varchar(140));

Food Service Items:

item (itemID varchar(20), name varchar(80), description varchar(140), category varchar(80), price double(), foodServiceID varchar(20), [fields int]);

Fields:
vegetarian,vegan
milk,eggs,peanuts,treeNuts,soy,wheatGluten,fish,shellFish

